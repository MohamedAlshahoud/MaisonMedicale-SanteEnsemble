{% extends 'base.html.twig' %}

{% block title %}Accueil - Centre Médical{% endblock %}

{% block body %}
    <!-- Section Héro -->
    <header class="bg-primary text-white text-center py-5">
        <div class="container">
            <h1 class="display-4">Bienvenue sur notre centre médical </h1>
            <p class="lead">Prenez rendez-vous avec nos médecins en ligne en quelques clics.</p>
            <a href="{{ path('rendez_vous_step_1') }}" class="btn btn-light btn-lg">Prendre un rendez-vous</a>
        </div>
    </header>

    <!-- Section des médecins disponibles -->
    <section class="container mt-5">
        <h2 class="text-center">Nos médecins disponibles</h2>
        <div class="row">
            {% for medecin in medecins %}
                <div class="col-md-4">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">{{ medecin.nom }} {{ medecin.prenom }}</h5>
                            <p class="card-text">Médecin généraliste</p>
                        </div>
                    </div>
                </div>
            {% else %}
                <p class="text-center">Aucun médecin disponible pour l'instant.</p>
            {% endfor %}
        </div>
    </section>
{% endblock %}
